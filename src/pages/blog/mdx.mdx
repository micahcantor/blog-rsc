import Layout from '../../components/ArticleLayout';
import MathBlock from '../../components/MathBlock';
import CodeBlock from '../../components/CodeBlock';

export default Layout;
export { CodeBlock, MathBlock };
export const metadata = {
  title: "Test article",
  description: "This is an article that contains writing and words.",
  tags: ["tag1", "tag2"],
}

# MDX page

This page is generated from an MDX file.

## Bright Code

Here is a Bright code block:

```py lineNumbers title="hello.py"
print("Hello World")
```

And here is some inline code: `InlineCode`.

## Katex

Here is a Katex math block:

<MathBlock tex="f(x) = x^2" displayMode={true} />

And here is some inline Katex: <MathBlock tex="f(5) = 25" />
